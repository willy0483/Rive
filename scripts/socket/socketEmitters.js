import { ioSocket } from "./socketController.js";

/**
 * This file contains all the emitters. If we need to emit (send) something to the backend server
 * that isnt within an action (something triggered by the user doing something), this is the place to do it.
 */
